import re
s = input()
k = input()
pattern = re.compile(f'(?={k})')
matches = list(pattern.finditer(s))
if not matches:
    print("(-1, -1)")
else:
    for m in matches:
        start = m.start()
        end = start + len(k) - 1
        print(f"({start}, {end})")
