import re
n = int(input())
pattern = r'(?<=[:\s])#(?:[0-9a-fA-F]{6}|[0-9a-fA-F]{3})(?![0-9a-fA-F])'

for _ in range(n):
    line = input()
    matches = re.findall(pattern, line)
    if matches:
        for match in matches:
            print(match)
